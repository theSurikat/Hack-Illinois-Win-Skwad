//var pg = require('pg');
//var connectionString = process.env.DATABASE_URL || 'postgres://localhost:5432/covalent';

//var client = new pg.Client(connectionString);
//client.connect();
//var query = client.query('CREATE TABLE partnerships(userId1 VARCHAR(100) not null, userId2 VARCHAR(100) not null, partnerShipId SERIAL PRIMARY KEY)');
//query.on('end', function() { client.end(); });
